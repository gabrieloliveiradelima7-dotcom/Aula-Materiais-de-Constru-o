# Frontend - ConstruManager

Aplicação React + Vite responsável pela interface.

## Executar

```bash
npm install
npm run dev
```

A aplicação consome a API via `/api` e usa proxy para `http://localhost:3001` no desenvolvimento.

Se necessário, configure em `.env`:

```bash
VITE_API_URL=http://localhost:3001
```
